import Blank from ".";
import type { Meta } from "@storybook/react";

const meta: Meta = {
  title: "fallback/Blank",
  component: Blank,
};

export default meta;

export const Default = {
  args: {},
};
